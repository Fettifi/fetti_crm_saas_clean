.vercel
.env.local
logs/
node_modules/
.next/
fetti_last_errors.json
.env*.local
.env
public/*.pdf
.claude/settings.local.json
scratchpad/
tsconfig.tsbuildinfo

# Redacted borrower training data — NEVER commit.
# LEADING SLASH IS LOAD-BEARING: a bare "dataset/" matches at ANY depth and silently
# excluded scripts/dataset/build.ts too, so the miner never got committed.
/dataset/

# Throwaway diagnostic scripts (agents + ad-hoc DB probes) — never committed
scripts/_tmp-*
